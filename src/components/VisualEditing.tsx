"use client";

// This component is intentionally empty.
// Visual Editing ONLY happens inside Sanity Studio's Presentation Tool iframe.
// The public website must NEVER show any CMS overlay, badge, or editing UI.
export function VisualEditing() {
  return null;
}
